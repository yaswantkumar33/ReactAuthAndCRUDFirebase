import React, { useState } from "react";
import { db, auth } from "../config/firebase";

export default function Allfunctions() {
  const [MovieList, SermovieLIst] = useState([]);
  const [updatedtittle, setupdatedtittle] = useState("");
  return <></>;
}
